import requests from '../Requests';

const Main = () => {
    const {movies, setMovies} = useState([]);

    useEffec

    return (
        <div>
            Main
        </div>
    )
}

export default Main;